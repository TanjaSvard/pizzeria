@model IEnumerable<PizzeriaMassagotti.Models.Dish>
@inject PizzeriaMassagotti.Services.CartService cartService

<h2>Your order</h2>
<h6>@cartService.Total()</h6>

@{
    ViewData["Title"] = "Meny";
}

@*@foreach (var item in Model)
{
<div class="col-sm-4 col-lg-4 col-md-4">
    <div class="thumbnail">
        @*<img src="@Model.ImageThumbnailUrl" alt="">*@
        @*<img src="http://placehold.it/500x300" alt="">*@
@*<div class="caption">

                <h4 class="pull-right">@Html.DisplayFor(modelItem => item.Price)</h4>
                <h4>
                    <a>@Html.DisplayFor(modelItem => item.Name)</a>
                </h4>
    <h6>
        <a asp-action="Edit" asp-route-id="@item.DishId">Edit</a> |
        <a asp-action="Details" asp-route-id="@item.DishId">Details</a> |
        <a asp-action="Delete" asp-route-id="@item.DishId">Delete</a>
    </h6>*@


@*@foreach (var i in item.DishIngredients)
{
                <h6>@i.Ingredient.Name</h6>
}
            </div>
        </div>
    </div>
}*@
@*Det ursprungliga utseende*@
<h2>Index</h2>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                Ingredients
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @foreach (var i in item.DishIngredients)
                    {
                        @i.Ingredient.Name
                }

                </td>
                <td>@Html.DisplayFor(modelItem => item.Category.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.DishId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.DishId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.DishId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
